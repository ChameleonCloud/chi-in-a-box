---
# Create a network, or run in check mode if it already exists.
# Required vars:
#   network_name: name of the network
#   network_args: dict of args passed to os_network (excluding auth/state/name)
- name: "Check if network '{{ network_name }}' exists"
  become: true
  run_once: true
  kolla_toolbox:
    container_engine: "{{ kolla_container_engine }}"
    module_name: openstack.cloud.networks_info
    module_args:
      auth: "{{ openstack_auth }}"
      name: "{{ network_name }}"
  register: _net_check

- name: "Create or update network '{{ network_name }}'"
  become: true
  run_once: true
  check_mode: "{{ _net_check.networks | length > 0 }}"
  kolla_toolbox:
    container_engine: "{{ kolla_container_engine }}"
    module_name: os_network
    module_args: "{{ {'auth': openstack_auth, 'name': network_name, 'state': 'present'} | combine(network_args) }}"
