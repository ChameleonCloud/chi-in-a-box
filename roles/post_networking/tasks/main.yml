---
- name: configure predefined networks
  include_tasks: config-network.yml
  tags: [networks]
  loop: "{{ neutron_networks }}"
  loop_control:
    label: "{{ item.name }}"
- name: configure sharednet router
  tags: [networks]
  include_tasks: sharednet-router.yml
