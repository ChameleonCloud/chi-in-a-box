---
hammers_config_path: /etc/hammers
hammers_docker_image: "ghcr.io/chameleoncloud/hammers:latest"
hammers_env_file: /etc/hammers/hammers.env
hammers_kubeconfig_path: /etc/hammers/kubeconfig.yml
hammers_mysql_user: cc_hammers
hammers_mysql_host: "{{ database_address }}"

hammers:
  # Networking/Neutron
  floating_ip_reaper:
    cmd: floatingip-reaper --grace-days 7
    calendar: daily
    enabled: false
  conflict_macs:
    cmd: conflict-macs delete
    calendar: daily
    enabled: false

  # Compute/Nova
  undead_instances:
    cmd: undead-instances delete
    calendar: daily
    enabled: false

  # Bare metal/Ironic
  dirty_ports:
    cmd: dirty-ports clean --multiport
    calendar: daily
    enabled: "{{ hammers_enable_dirty_ports | bool }}"
  ironic_error_resetter:
    cmd: ironic-error-resetter reset
    calendar: hourly
    enabled: "{{ hammers_enable_ironic_error_resetter | bool }}"
  enforce_node_retirement:
    cmd: "retirement-enforcer"
    calendar: daily
    enabled: false

  # Reservation/Blazar
  orphan_resource_providers:
    cmd: orphan-resource-providers update
    calendar: daily
    enabled: false
  reservation_usage_notification:
    cmd: "reservation-usage-notification"
    calendar: daily
    enabled: false
  orphans_detector:
    cmd: "orphans-detector"
    calendar: daily
    enabled: false
  clean_old_aggregates:
    cmd: "clean-old-aggregates"
    calendar: daily
    enabled: false
  lease_stacking:
    cmd: "lease-stack-notifier notify --config {{ hammers_config_path }}/lease-stacking-config.json"
    calendar: daily
    enabled: "{{ hammers_enable_lease_stacking | bool }}"
  unutilized_leases:
    cmd: "unutilized-lease-reaper delete"
    calendar: hourly
    enabled: "{{ hammers_enable_unutilized_leases_baremetal | bool }}"

  # Kubernetes/Edge
  calico_ip_cleanup:
    cmd: "k8s-calico-ip-cleanup --kubeconfig_path {{ hammers_kubeconfig_path }}"
    calendar: daily
    enabled: "{{ hammers_enable_calico_ip_cleanup | bool}}"

hammers_slack_webhook: "{{ slack_api_url }}"

hammers_v2_docker_image: ghcr.io/chameleoncloud/chameleon_site_tools:main
hammers_v2:
  floating_ip_network_reaper:
    description: "Clean up networks for expired projects."
    env_file: "{{ hammers_env_file }}"
    image: "{{ hammers_v2_docker_image }}"
    cmd: floating_ip_network_reaper --cloud envvars --grace-days 7  --clean-floatingips --clean-routers
    enabled: "{{ hammers_enable_floating_ip_network_reaper | bool }}"
    calendar: daily
